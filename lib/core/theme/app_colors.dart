import 'package:flutter/material.dart';

class AppColors {
  // Cinematic Dark Backgrounds
  static const Color background = Color(0xFF090A10);
  static const Color surface = Color(0xFF131522);
  static const Color surfaceElevated = Color(0xFF1B1E30);

  // Vibrant Premium Accents
  static const Color primary = Color(0xFFFA2E76); // Neon pink/red for CTA & hearts
  static const Color secondary = Color(0xFF8A2BE2); // Electric Violet for AI sparkles
  static const Color accent = Color(0xFF00F5FF); // Electric cyan

  // Translucent Glassmorphic properties
  static const Color glassBackground = Color(0x1AFFFFFF);
  static const Color glassBorder = Color(0x33FFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFFF5F5F7);
  static const Color textSecondary = Color(0xFF9E9EAF);
  static const Color textMuted = Color(0xFF6E6E80);

  // Semantic Status
  static const Color success = Color(0xFF00E676);
  static const Color warning = Color(0xFFFFA000);
  static const Color error = Color(0xFFFF1744);
}
