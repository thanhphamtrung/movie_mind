import 'package:flutter/material.dart';

class AppColors {
  // Cinematic Dark Backgrounds
  static const Color background = Color(0xFF191B24);
  static const Color surface = Color(0xFF212529);
  static const Color surfaceElevated = Color(0xFF282B3A);

  // Vibrant Premium Accents
  static const Color primary = Color(0xFFDC3545); // Vibrant Red for CTAs
  static const Color secondary = Color(0xFF0D6EFD); // Blue for active links, highlights
  static const Color accent = Color(0xFFFFC107); // Gold/Yellow for star ratings

  // Translucent Glassmorphic properties
  static const Color glassBackground = Color(0x1AFFFFFF);
  static const Color glassBorder = Color(0x33FFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFF8F9FA);
  static const Color textMuted = Color(0xFF6C757D);

  // Semantic Status
  static const Color success = Color(0xFF198754);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFDC3545);
  static const Color info = Color(0xFF0DCAF0);
}
